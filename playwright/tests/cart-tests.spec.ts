import { expect } from '@playwright/test';
import { test } from '../fixtures/fixture';

test.describe('', () => {
  test.beforeEach(async ({}) => {});

  test('Start Ordering button should show when the user full name is entered', async ({}) => {});

  test('Start Ordering button should show when the user full name is entered', async ({}) => {});
});
